This project is a back-end part of js front-end project eLibrary. Instead of external API's in this project created REST API's 
in backend (nodejs) is invoked from frontend via AJAX and represens all selected items (books) from MySql database (CRUD).  
